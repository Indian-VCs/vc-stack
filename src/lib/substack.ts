/**
 * Public Substack newsletter URL for the VC Stack publication.
 * Not a secret — lives in code so env wiring isn't required for deploy.
 */
export const SUBSTACK_URL = 'https://thevcstack.substack.com'
